<UserControl x:Class="DtHub.App.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel Margin="28,26,28,26">

    <StackPanel DockPanel.Dock="Top">
      <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitle}" />
      <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitle}" />

      <Border Style="{StaticResource Card}" Background="{DynamicResource AccentSoftBrush}"
              BorderBrush="{DynamicResource AccentBrush}"
              Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVisibility}}">
        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" />
      </Border>
    </StackPanel>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="280" />
        <ColumnDefinition Width="14" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <!-- Liste des profils -->
      <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0">
        <DockPanel>
          <Button DockPanel.Dock="Top" Content="+  Nouveau profil" Style="{StaticResource PrimaryButton}"
                  Command="{Binding CreateCommand}" Margin="0,0,0,12" />

          <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}"
                   BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <StackPanel Margin="2,6">
                  <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding Summary}" Style="{StaticResource Muted}" FontSize="11" />
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>

      <!-- Détail du profil -->
      <Border Grid.Column="2" Style="{StaticResource Card}" Margin="0"
              Visibility="{Binding HasProfile, Converter={StaticResource BoolToVisibility}}">
        <DockPanel>
          <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="{Binding SelectedProfile.Name}" FontSize="17" FontWeight="SemiBold" />
            <TextBlock Text="{Binding SelectedProfile.Summary}" Style="{StaticResource Muted}" Margin="0,2,0,12" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
              <Button Content="Renommer" Command="{Binding RenameCommand}" Margin="0,0,6,0" />
              <Button Content="Dupliquer" Command="{Binding DuplicateCommand}" Margin="0,0,6,0" />
              <Button Content="Profil par défaut" Command="{Binding SetAsDefaultCommand}" Margin="0,0,6,0"
                      IsEnabled="{Binding IsSelectedProfileDefault, Converter={StaticResource InverseBool}}" />
              <Button Content="Supprimer" Style="{StaticResource DangerButton}" Command="{Binding DeleteCommand}" />
            </StackPanel>

            <Border Style="{StaticResource Separator}" />

            <TextBlock Text="SESSIONS, DANS L'ORDRE D'OUVERTURE" Style="{StaticResource SectionTitle}" />
          </StackPanel>

          <ListBox ItemsSource="{Binding Targets}" SelectedItem="{Binding SelectedTarget}"
                   BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Grid Margin="0,4">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>

                  <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DisplayName}" />
                    <TextBlock Text="{Binding PackageName}" Style="{StaticResource Muted}" FontSize="11" />
                  </StackPanel>

                  <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="↑" Width="30" Style="{StaticResource LinkButton}"
                            ToolTip="Monter dans l'ordre"
                            Command="{Binding DataContext.MoveTargetUpCommand,
                                              RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}" />
                    <Button Content="↓" Width="30" Style="{StaticResource LinkButton}"
                            ToolTip="Descendre dans l'ordre"
                            Command="{Binding DataContext.MoveTargetDownCommand,
                                              RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}" />
                    <Button Content="✕" Width="30" Style="{StaticResource LinkButton}"
                            ToolTip="Retirer du profil"
                            Command="{Binding DataContext.RemoveTargetCommand,
                                              RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}" />
                  </StackPanel>
                </Grid>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>

      <Border Grid.Column="2" Style="{StaticResource Card}" Margin="0" VerticalAlignment="Top"
              Visibility="{Binding HasProfile, Converter={StaticResource InverseBoolToVisibility}}">
        <TextBlock TextWrapping="Wrap" Style="{StaticResource Muted}"
                   Text="Aucun profil pour l'instant. Créez-en un, puis ajoutez-y des applications depuis la page Applications." />
      </Border>

    </Grid>
  </DockPanel>
</UserControl>
