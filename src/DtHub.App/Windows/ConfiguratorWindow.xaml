<Window x:Class="DtHub.App.Windows.ConfiguratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:w="clr-namespace:DtHub.App.Windows"
        Title="DT Hub"
        Width="400" Height="580"
        MinWidth="360" MinHeight="380"
        Topmost="True"
        ShowInTaskbar="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        TextOptions.TextFormattingMode="Display">

  <Border Background="{DynamicResource SurfaceAltBrush}"
          BorderBrush="{StaticResource BevelBorderBrush}" BorderThickness="1"
          CornerRadius="{StaticResource Radius}">
    <DockPanel>

      <!-- Bandeau : sert aussi de poignée de déplacement. -->
      <Border DockPanel.Dock="Top" Background="{StaticResource BrushedPanelBrush}"
              CornerRadius="{StaticResource RadiusTop}" Padding="12,8"
              BorderBrush="{DynamicResource GrooveBrush}" BorderThickness="0,0,0,1"
              MouseLeftButtonDown="OnHeaderDrag">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <Image Grid.Column="0" Source="/assets/app.png" Width="26" Height="26" VerticalAlignment="Center" />

          <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="{Binding ProductName}" FontSize="17" FontWeight="SemiBold" />
            <TextBlock Style="{StaticResource Muted}" FontSize="11">
              <Run Text="{Binding ToggleShortcutText, Mode=OneWay}" FontWeight="SemiBold"
                   FontFamily="{StaticResource MonoFont}" />
              <Run Text=" pour afficher ou masquer" />
            </TextBlock>
          </StackPanel>

          <Button Grid.Column="2" Content="✕" Style="{StaticResource QuietButton}"
                  FontSize="14" Padding="8,2" Click="OnHide"
                  ToolTip="Masquer cette fenêtre" />
        </Grid>
      </Border>

      <!-- Ce que la prochaine version apporte. Le bandeau ne paraît que
           lorsqu'il y a quelque chose à dire, et il le dit en une ligne : la
           note complète tient dans sa propre fenêtre, qu'on ouvre si on veut. -->
      <Border DockPanel.Dock="Top" Padding="12,7"
              Background="{StaticResource BrushedPanelBrush}"
              BorderBrush="{DynamicResource GrooveBrush}" BorderThickness="0,0,0,1"
              Visibility="{Binding UpdateText, Converter={StaticResource EmptyToVisibility},
                                   ConverterParameter=inverse}">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <TextBlock Grid.Column="0" VerticalAlignment="Center" TextWrapping="Wrap"
                     FontSize="12" Text="{Binding UpdateText}" />

          <Button Grid.Column="1" Content="Nouveautés" Style="{StaticResource QuietButton}"
                  Padding="8,3" Margin="8,0,0,0" VerticalAlignment="Center"
                  Command="{Binding UpdateNotesCommand}"
                  ToolTip="Lire ce que cette version apporte" />
        </Grid>
      </Border>

      <!-- Barre du bas : outils à gauche, quitter à droite. -->
      <Border DockPanel.Dock="Bottom" Padding="10,8"
              Background="{StaticResource BrushedPanelBrush}"
              BorderBrush="{DynamicResource BevelBrush}" BorderThickness="0,1,0,0">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <!-- Les outils viendront s'ajouter ici, chacun avec son raccourci. -->
          <!-- Le raccourci passe sous son bouton : d'autres outils viendront se
               ranger à sa droite, sur la même ligne. -->
          <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
            <StackPanel MinWidth="54">
            <Button Padding="8,5" Style="{StaticResource QuietButton}" HorizontalAlignment="Center"
                    ToolTip="Empiler toutes les fenêtres de jeu sur la dernière que vous avez utilisée, à sa position et à sa taille"
                    Command="{Binding RearrangeCommand}">
              <!-- Deux fenêtres superposées : c'est exactement ce que fait
                   l'action, et aucun glyphe ne le dit clairement. -->
              <Grid Width="20" Height="16">
                <Border Width="14" Height="11" CornerRadius="2"
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        BorderThickness="1.4" BorderBrush="{DynamicResource TextMutedBrush}" />
                <Border Width="14" Height="11" CornerRadius="2"
                        HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Background="{DynamicResource SurfaceAltBrush}"
                        BorderThickness="1.4" BorderBrush="{DynamicResource AccentBrush}" />
              </Grid>
            </Button>

            <TextBlock Text="{Binding RearrangeShortcutText}" Style="{StaticResource Muted}" FontFamily="{StaticResource MonoFont}"
                       FontSize="10" HorizontalAlignment="Center" Margin="0,1,0,0"
                       Visibility="{Binding RearrangeShortcutText,
                                            Converter={StaticResource EmptyToVisibility},
                                            ConverterParameter=inverse}" />
            </StackPanel>

            <StackPanel MinWidth="54" Margin="14,0,0,0">
              <Button Padding="8,5" Style="{StaticResource QuietButton}" HorizontalAlignment="Center"
                      ToolTip="Ranger deux fenêtres côte à côte, chacune sur une moitié de l'écran. La fenêtre active va à droite."
                      Command="{Binding TileCommand}">
                <!-- Deux fenêtres accolées, l'active en couleur à droite. -->
                <Grid Width="20" Height="16">
                  <Border Width="9" Height="13" CornerRadius="2"
                          HorizontalAlignment="Left" VerticalAlignment="Center"
                          BorderThickness="1.4" BorderBrush="{DynamicResource TextMutedBrush}" />
                  <Border Width="9" Height="13" CornerRadius="2"
                          HorizontalAlignment="Right" VerticalAlignment="Center"
                          Background="{DynamicResource SurfaceAltBrush}"
                          BorderThickness="1.4" BorderBrush="{DynamicResource AccentBrush}" />
                </Grid>
              </Button>

              <TextBlock Text="{Binding TileShortcutText}" Style="{StaticResource Muted}" FontFamily="{StaticResource MonoFont}"
                         FontSize="10" HorizontalAlignment="Center" Margin="0,1,0,0"
                         Visibility="{Binding TileShortcutText,
                                              Converter={StaticResource EmptyToVisibility},
                                              ConverterParameter=inverse}" />
            </StackPanel>

            <StackPanel MinWidth="54" Margin="14,0,0,0">
              <Button Padding="8,5" Style="{StaticResource QuietButton}" HorizontalAlignment="Center"
                      ToolTip="Ouvrir les guides, qui restent au-dessus des fenêtres de jeu. Quêtes, donjons, raids, tanières et chemins, de papycha.fr."
                      Command="{Binding QuestsCommand}">
                <!-- Un parchemin déroulé : deux rouleaux et des lignes de
                     texte. L'icône est à nous ; celle du site ne sera reprise
                     que s'ils l'accordent. -->
                <Grid Width="20" Height="16">
                  <Border Width="3" Height="14" CornerRadius="{StaticResource Radius}"
                          HorizontalAlignment="Left" VerticalAlignment="Center"
                          BorderThickness="1.4" BorderBrush="{DynamicResource TextMutedBrush}" />
                  <Border Width="3" Height="14" CornerRadius="{StaticResource Radius}"
                          HorizontalAlignment="Right" VerticalAlignment="Center"
                          BorderThickness="1.4" BorderBrush="{DynamicResource TextMutedBrush}" />
                  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="9">
                    <Border Height="1.4" Margin="0,0,0,3" Background="{DynamicResource AccentBrush}" />
                    <Border Height="1.4" Margin="0,0,0,3" Background="{DynamicResource TextMutedBrush}" />
                    <Border Height="1.4" Background="{DynamicResource TextMutedBrush}" />
                  </StackPanel>
                </Grid>
              </Button>

              <TextBlock Text="{Binding QuestsShortcutText}" Style="{StaticResource Muted}" FontFamily="{StaticResource MonoFont}"
                         FontSize="10" HorizontalAlignment="Center" Margin="0,1,0,0"
                         Visibility="{Binding QuestsShortcutText,
                                              Converter={StaticResource EmptyToVisibility},
                                              ConverterParameter=inverse}" />
            </StackPanel>
          </StackPanel>

          <StackPanel Grid.Column="1">
            <Button Content="Quitter" Click="OnQuit"
                    ToolTip="Fermer l'application et les fenêtres de jeu" />
            <TextBlock Text="{Binding QuitShortcutText}" Style="{StaticResource Muted}" FontFamily="{StaticResource MonoFont}"
                       FontSize="10" HorizontalAlignment="Center" Margin="0,1,0,0"
                       Visibility="{Binding QuitShortcutText,
                                            Converter={StaticResource EmptyToVisibility},
                                            ConverterParameter=inverse}" />
          </StackPanel>
        </Grid>
      </Border>

      <!-- Onglets -->
      <Grid Margin="10,8,10,0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
          <RadioButton x:Name="TabHotkeys" Style="{StaticResource Tab}" Content="Raccourcis"
                       GroupName="Tabs" IsChecked="True" />
          <RadioButton x:Name="TabGeneral" Style="{StaticResource Tab}" Content="Fenêtres" GroupName="Tabs" />
          <RadioButton x:Name="TabDevices" Style="{StaticResource Tab}" Content="Appareils" GroupName="Tabs" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,10,0">
          <Grid>

            <!-- Onglet Raccourcis, en lecture seule -->
            <StackPanel Visibility="{Binding IsChecked, ElementName=TabHotkeys,
                                             Converter={StaticResource BoolToVisibility}}">
              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <ItemsControl ItemsSource="{Binding Hotkeys}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,2" Background="Transparent"
                              ToolTip="{Binding ActionDetail}">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>

                          <TextBlock Grid.Column="0" Text="{Binding ActionLabel}" VerticalAlignment="Center"
                                     TextTrimming="CharacterEllipsis" Margin="0,0,8,0" />

                          <Border Grid.Column="1" CornerRadius="{StaticResource Radius}" Padding="7,2"
                                  Background="{DynamicResource SurfaceAltBrush}"
                                  BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
                            <TextBlock Text="{Binding ShortcutText}" FontSize="12"
                       FontFamily="{StaticResource MonoFont}" />
                          </Border>
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>

                  <Button Content="Modifier les raccourcis" HorizontalAlignment="Left" Margin="0,8,0,0"
                          Click="OnEditHotkeys" />
                </StackPanel>
              </Border>
            </StackPanel>

            <!-- Onglet Fenêtres : replacement rapide et taille -->
            <StackPanel Visibility="{Binding IsChecked, ElementName=TabGeneral,
                                             Converter={StaticResource BoolToVisibility}}">

              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <TextBlock Text="REPLACER LES FENÊTRES" Style="{StaticResource SectionLabel}" />

                  <ItemsControl ItemsSource="{Binding Anchors}" HorizontalAlignment="Left"
                                Margin="0,4,0,0"
                                ToolTip="Cliquez une case pour regrouper toutes les fenêtres à cet endroit de l'écran. C'est aussi le coin où s'ouvrent les fenêtres qui n'ont pas encore de place retenue.">
                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <UniformGrid Columns="3" Rows="3" />
                      </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Button Style="{StaticResource AnchorCell}"
                                ToolTip="{Binding Converter={StaticResource AnchorLabel}}"
                                Command="{Binding DataContext.SetAnchorCommand,
                                                  RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}" />
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>

                </StackPanel>
              </Border>

              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <TextBlock Text="TAILLE DES FENÊTRES" Style="{StaticResource SectionLabel}"
                             ToolTip="Proportionnelle à la zone utilisable de l'écran, et non à ses pixels : la même valeur donne la même part d'écran partout. Les raccourcis Ctrl+1 à Ctrl+5 posent cinq repères sur ce curseur." />

                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Slider Grid.Column="0" Minimum="20" Maximum="100" TickFrequency="1"
                            SmallChange="1" LargeChange="5"
                            Value="{Binding SizePercent}" />

                    <!-- En chasse fixe : le nombre ne danse plus d'un pixel à
                         l'autre quand il change de chiffre. -->
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"
                               MinWidth="46" TextAlignment="Right" FontWeight="SemiBold"
                               FontFamily="{StaticResource MonoFont}"
                               Foreground="{DynamicResource AccentBrush}">
                      <Run Text="{Binding SizePercent, Mode=OneWay}" /><Run Text=" %" />
                    </TextBlock>
                  </Grid>
                </StackPanel>
              </Border>

              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <TextBlock Text="QUALITÉ" Style="{StaticResource SectionLabel}" />

                  <!-- Repliés comme la distance juste en dessous : quatre
                       paliers ne tiennent plus sur la largeur de la fenêtre. -->
                  <WrapPanel Margin="0,0,0,5">
                    <RadioButton Style="{StaticResource Tab}" GroupName="Quality" Content="Basse"
                                 Margin="0,0,5,0"
                                 ToolTip="Image en 720 au plus, 30 images par seconde, débit réduit, et le téléphone est interrogé deux fois moins souvent. Pour les machines et les téléphones modestes, ou beaucoup de comptes à la fois."
                                 IsChecked="{Binding Quality, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Low}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Quality" Content="Moyenne"
                                 Margin="0,0,5,0"
                                 ToolTip="Image en 1080 au plus, 60 images par seconde. Le réglage d'origine, qui convient à la plupart des machines."
                                 IsChecked="{Binding Quality, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Medium}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Quality" Content="Maximale"
                                 ToolTip="Image à la définition de la fenêtre, sans aucune borne, 120 images par seconde et débit doublé encore. Sans ménagement pour le téléphone : à réserver à un appareil récent, et en Wi-Fi à un réseau qui suit."
                                 IsChecked="{Binding Quality, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Maximum}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Quality" Content="Personnalisé"
                                 Margin="5,0,0,0"
                                 ToolTip="Régler soi-même la définition, la cadence, le débit et le codec. Les trois paliers ci-contre restent le chemin ordinaire : celui-ci sert à qui sait déjà ce qu'il veut."
                                 IsChecked="{Binding Quality, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Custom}" />
                  </WrapPanel>

                  <!-- Replié tant que le palier personnalisé n'est pas choisi :
                       montrer ces quatre lignes en permanence chargerait le
                       panneau de ce que la plupart des gens n'ont pas à savoir. -->
                  <StackPanel Margin="0,8,0,0"
                              Visibility="{Binding IsCustomQuality,
                                                   Converter={StaticResource BoolToVisibility}}">
                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Définition max." VerticalAlignment="Center"
                                 Style="{StaticResource Muted}"
                                 ToolTip="Un plafond, non la définition retenue : celle-ci suit la taille de la fenêtre et peut être moindre." />
                      <ComboBox Grid.Column="1" ItemsSource="{Binding HeightChoices}"
                                SelectedValuePath="Value"
                                SelectedValue="{Binding CustomHeight}">
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Label}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                      </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Cadence" VerticalAlignment="Center"
                                 Style="{StaticResource Muted}" />
                      <ComboBox Grid.Column="1" ItemsSource="{Binding FpsChoices}"
                                SelectedValuePath="Value"
                                SelectedValue="{Binding CustomFps}">
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Label}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                      </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Finesse d'image" VerticalAlignment="Center"
                                 Style="{StaticResource Muted}"
                                 ToolTip="Bits accordés à chaque pixel de chaque image. Le débit s'en déduit d'après la définition réellement retenue, comme pour les trois autres paliers : un débit absolu servirait grassement une petite fenêtre et affamerait une grande." />
                      <ComboBox Grid.Column="1" ItemsSource="{Binding FinesseChoices}"
                                SelectedValuePath="Value"
                                SelectedValue="{Binding CustomBitsPerPixel}">
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Label}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                      </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="Codec vidéo" VerticalAlignment="Center"
                                 Style="{StaticResource Muted}" />
                      <ComboBox Grid.Column="1" ItemsSource="{Binding CodecChoices}"
                                SelectedValuePath="Value"
                                SelectedValue="{Binding CustomCodec}"
                                ToolTip="AV1 et VP8 ne sont pas proposés : sur un téléphone ordinaire ils n'ont pas d'encodeur matériel, et les encoder en logiciel coûte bien plus qu'ils ne rendent.">
                        <ComboBox.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Text="{Binding Label}" />
                          </DataTemplate>
                        </ComboBox.ItemTemplate>
                      </ComboBox>
                    </Grid>

                    <!-- Un débit nu ne veut rien dire : seize mégabits sont
                         généreux en 720p et misérables en 2160p. Cette ligne
                         dit ce que l'encodeur reçoit vraiment. -->
                    <TextBlock Text="{Binding BitrateSummary}" FontWeight="SemiBold"
                               FontFamily="{StaticResource MonoFont}" FontSize="11"
                               Foreground="{DynamicResource AccentBrush}"
                               ToolTip="Bits accordés à chaque pixel de chaque image. La référence du métier tourne autour de 0,10 pour du H.264 de bonne facture." />

                    <!-- Plusieurs comptes ouverts, ce sont plusieurs flux sur
                         la même liaison et le même encodeur. Un miroir simple
                         n'a pas ce souci ; celui-ci l'a, et le dit. -->
                    <TextBlock Text="{Binding LinkSummary}" Style="{StaticResource Muted}"
                               FontFamily="{StaticResource MonoFont}" FontSize="11"
                               TextWrapping="Wrap"
                               ToolTip="Ce que le téléphone devra pousser sur le Wi-Fi. Un appareil en Wi-Fi 4 sur 2,4 GHz en rend une soixantaine de mégabits en pratique." />
                  </StackPanel>
                </StackPanel>
              </Border>

              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <TextBlock Text="DISTANCE DANS LE JEU" Style="{StaticResource SectionLabel}"
                             ToolTip="Agit sur la densité de l'écran vu par Android, et non sur la finesse de l'image : à taille de fenêtre égale, le jeu montre plus ou moins de terrain." />

                  <!-- Repliés plutôt qu'alignés : cinq paliers ne tiennent
                       plus sur la largeur de la fenêtre, et rogner un libellé
                       le rendrait indéchiffrable. -->
                  <WrapPanel Margin="0,0,0,5">
                    <RadioButton Style="{StaticResource Tab}" GroupName="Zoom" Content="Très éloignée"
                                 Margin="0,0,5,0"
                                 ToolTip="Le plus de terrain qu'il soit possible d'afficher. L'interface du jeu devient petite : sur une fenêtre modeste, elle sera à la limite du lisible."
                                 IsChecked="{Binding Zoom, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Widest}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Zoom" Content="Éloignée"
                                 Margin="0,0,5,0"
                                 ToolTip="Beaucoup de terrain visible, l'interface du jeu petite. Pratique pour suivre plusieurs comptes d'un coup d'oeil, moins confortable pour viser."
                                 IsChecked="{Binding Zoom, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Wide}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Zoom" Content="Normale"
                                 Margin="0,0,5,0"
                                 ToolTip="Le réglage d'origine, celui d'un affichage de 1080 pixels à 240 ppp."
                                 IsChecked="{Binding Zoom, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Normal}" />
                    <RadioButton Style="{StaticResource Tab}" GroupName="Zoom" Content="Proche"
                                 ToolTip="Le moins de terrain possible, l'interface du jeu la plus grande. Confortable sur une petite fenêtre. Sur une fenêtre de plus de 2300 pixels de haut, ce palier se rapproche du précédent : la densité demandée y touche son plafond."
                                 IsChecked="{Binding Zoom, Converter={StaticResource EnumMatch},
                                                     ConverterParameter=Close}" />
                  </WrapPanel>

                  <TextBlock Style="{StaticResource Muted}" FontSize="11"
                             Visibility="{Binding IsReopening, Converter={StaticResource BoolToVisibility}}"
                             Text="Les fenêtres ouvertes se rouvrent pour prendre le changement." />
                </StackPanel>
              </Border>

              <Border Style="{StaticResource Card}">
                <StackPanel>
                  <TextBlock Text="SUR LE TÉLÉPHONE" Style="{StaticResource SectionLabel}" />

                  <CheckBox Content="Son du téléphone sur le PC" Margin="0,0,0,8"
                            IsChecked="{Binding AudioEnabled}"
                            ToolTip="Android ne sait pas isoler le son d'une application : c'est celui du téléphone entier qui arrive. Une seule fenêtre par appareil le porte donc, sinon le même flux reviendrait en plusieurs exemplaires." />

                  <CheckBox Content="Éteindre l'écran du téléphone" Margin="0,0,0,8"
                            IsChecked="{Binding TurnScreenOff}"
                            ToolTip="L'écran de l'appareil s'éteint pendant les sessions. L'image continue d'arriver sur le PC." />

                  <!-- Le seul réglage du panneau qui touche le téléphone
                       lui-même. Ses trois valeurs sont relues avant d'être
                       changées, et rendues à la fermeture des fenêtres. -->
                  <CheckBox Content="Couper les animations du téléphone"
                            IsChecked="{Binding DisableAnimations}"
                            ToolTip="Réglage du téléphone, pas de la session : il est remis comme il était quand vous fermez les fenêtres. Si l'application s'arrête brutalement, les animations resteront coupées et il faudra les rétablir depuis les options pour développeurs." />
                </StackPanel>
              </Border>

              <StackPanel Margin="2,4,0,10">
                <!-- Le téléchargement se fait en fond, l'échange à l'arrêt : une
                     application ne se remplace pas sous les doigts de celui qui
                     s'en sert. -->
                <CheckBox Content="Se mettre à jour toute seule" Margin="0,0,0,8"
                          IsChecked="{Binding UpdatesAutomatic}"
                          ToolTip="La nouvelle version se télécharge en fond et s'installe quand vous quittez. Décoché, l'application se contente de dire qu'une version existe." />

                <TextBlock Style="{StaticResource Muted}" FontSize="11">
                  <Run Text="{Binding ProductName, Mode=OneWay}" />
                  <Run Text="{Binding Version, Mode=OneWay}" />
                </TextBlock>
                <TextBlock Text="{Binding Disclaimer}" Style="{StaticResource Muted}" FontSize="11" />
                <Button Content="Ouvrir le dossier des journaux" Style="{StaticResource QuietButton}"
                        HorizontalAlignment="Left" Margin="-9,4,0,0" Command="{Binding OpenLogsCommand}" />
              </StackPanel>

            </StackPanel>

            <!-- Onglet Appareils -->
            <StackPanel Visibility="{Binding IsChecked, ElementName=TabDevices,
                                             Converter={StaticResource BoolToVisibility}}">

              <Button Content="+  Associer un nouvel appareil" HorizontalAlignment="Left" Margin="0,0,0,12"
                      ToolTip="À faire une seule fois par appareil. Les appareils déjà associés se connectent tout seuls."
                      Click="OnAddDevice" />

              <!-- L'état du téléphone en toutes lettres. Une pastille de
                   couleur ne dit pas ce qu'elle reproche, et c'est au premier
                   lancement, quand rien ne marche encore, qu'on en a le plus
                   besoin. -->
              <w:InstanceListControl DataContext="{Binding Instances}"
                                     ShowDeviceStatus="True" />

            </StackPanel>

          </Grid>
        </ScrollViewer>

        <!-- Astuces de l'onglet Appareils, hors du contenu défilant : elles
             restent collées en bas de la page quelle que soit la longueur de
             la liste. La marge négative annule le remplissage et la bordure du
             bouton, neuf pixels, pour que le texte parte du même bord que la
             liste au-dessus. -->
        <StackPanel Grid.Row="2" Margin="0,6,0,0"
                    Visibility="{Binding IsChecked, ElementName=TabDevices,
                                         Converter={StaticResource BoolToVisibility}}">
          <Button Content="Une fenêtre se fige au bout d'un moment ?"
                  Style="{StaticResource QuietButton}" HorizontalAlignment="Left"
                  Margin="-9,0,0,0" Click="OnSleepHelp" />
        </StackPanel>
      </Grid>

    </DockPanel>
  </Border>
</Window>
